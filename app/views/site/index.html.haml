%h1= t('site.title')

- (@sections - Array(@sections.first)).each do |section|
  %h3= section.name
  - unless section.favorites.blank?
    %ul
      - section.favorites.each do |faves|
        %li=favorite_link_to(faves)
